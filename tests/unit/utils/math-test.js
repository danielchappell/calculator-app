/* jshint expr:true */
import { expect } from 'chai';
import {
  describe,
  it
} from 'mocha';
import math from 'calculator-app/utils/math';

describe('math', function() {
  // Replace this with your real tests.
  it('works', function() {
    var result = math();
    expect(result).to.be.ok;
  });
});
